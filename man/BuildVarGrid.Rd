% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit.R
\name{BuildVarGrid}
\alias{BuildVarGrid}
\title{Build the Dirichlet-multinomial variance-regularization grid}
\usage{
BuildVarGrid(
  param.peak,
  sump.peak,
  pfrac,
  gene.idx,
  gene.sum,
  background.cells,
  number.bins,
  sample.n
)
}
\arguments{
\item{param.peak}{fitted concentration parameter for each (kept) peak}

\item{sump.peak}{sum of concentration parameters for each peak's gene}

\item{pfrac}{\code{param.peak / sump.peak}, the background fraction for each peak}

\item{gene.idx}{row of \code{gene.sum} giving each peak's gene total}

\item{gene.sum}{genes x cells matrix of per-gene counts}

\item{background.cells}{cells used to estimate the background distribution}

\item{number.bins}{number of bins along each grid axis}

\item{sample.n}{max observations sampled per bin before kernel regression}
}
\value{
A list with the bin edges (\code{ec_grid}, \code{n_grid}), the regularized
variance at each grid cell (\code{grid.var}), the integer keys of those grid cells
(\code{grid.key}), and the key base (\code{key.base}) used to encode (ec, n) bins.
}
\description{
Computes expected counts and Dirichlet-multinomial variances for the background
cells, then fits a smooth variance surface over a regular
(gene-total, expected-count) grid via kernel regression. The returned grid is
used to regularize the variance of every cell when residuals are computed.
}
\concept{residuals}
